# shellcheck disable=all

# dbt target (optional, defaults to 'dev')
# DBT_TARGET="dev"   # Development schema (ecommerce_dev)
DBT_TARGET="prod"  # Production schema (ecommerce)

# Trino connection settings
TRINO_HOST="trino.example.com"  # Trino server hostname
TRINO_PORT="443"  # Trino server port (443 for HTTPS, 8080 for HTTP)
TRINO_HTTP_SCHEME="https"  # http or https
ICEBERG_CATALOG="iceberg"  # Iceberg catalog name in Trino
