# dbt environment variables for local development
# Copy this file to .env.local and fill in your credentials
#
# .env.local is gitignored - NEVER commit credentials to git!
#
# Usage:
#   cp .env.local.example .env.local
#   # Edit .env.local with your 1Password references (if using op CLI)
#   op run --env-file=.env.local -- dbt run

# Iceberg REST Catalog configuration
# Note: These variables are not used by dbt directly, but kept for reference
# to match the dlt ingestion configuration. Trino connects to Iceberg catalog
# directly via its own configuration.
ICEBERG_CATALOG_URL="http://lakekeeper.lakekeeper:8181/catalog"
ICEBERG_WAREHOUSE="default"
ICEBERG_NAMESPACE="ecommerce"

# dbt target (optional, defaults to 'dev')
# DBT_TARGET="dev"   # Development schema (ecommerce_dev)
# DBT_TARGET="prod"  # Production schema (ecommerce)

# Note: Trino authentication is handled via OAuth2 (browser-based flow)
# No username/password needed in this file
